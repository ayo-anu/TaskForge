"""Repository-owned task metadata and worker implementations.

This package is intentionally inert. Production processes load the metadata and
executable surfaces independently through their installed entry points.
"""
